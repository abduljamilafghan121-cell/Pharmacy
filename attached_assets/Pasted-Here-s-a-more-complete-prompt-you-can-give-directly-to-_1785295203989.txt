Here's a more complete prompt you can give directly to your coding agent.
Writing
Task: Implement Production-Ready Password Reset Email System for PharmaCore
Review the current implementation of lib/mailer.ts and replace any stub or console-based implementation with a production-ready email service using Nodemailer and Gmail SMTP.
Objectives
Implement a secure, reliable password reset email system suitable for production deployment on Vercel.
1. Install Dependencies
Ensure the following packages are installed:
nodemailer
@types/nodemailer
Update package.json if necessary.
2. Update lib/mailer.ts
Replace the existing implementation with a reusable Nodemailer transporter.
Requirements:
Use Gmail SMTP:
service: "gmail"
Authenticate using:
process.env.GMAIL_USER
process.env.GMAIL_PASS
Sender:
PharmaCore <process.env.GMAIL_USER>
Subject:
Reset Your Password – PharmaCore
The exported function should:
Accept:
recipient email
resetLink
Await transporter.sendMail()
Throw meaningful errors if sending fails.
Never log passwords or reset tokens.
Be fully compatible with Vercel Serverless Functions.
3. Email Template
Create a clean, professional HTML email using PharmaCore branding.
Include:
PharmaCore heading
Friendly greeting
Explanation that a password reset was requested
Large "Reset Password" button
Plain-text reset link beneath the button
Notice that the link expires (for example, after 1 hour)
Message to ignore the email if they did not request it
Footer:
© PharmaCore
Keep the design responsive and email-client friendly.
Also provide a plain text version of the email.
4. Password Reset Flow
Review the password reset route.
Verify it:
Looks up the user
Generates a cryptographically secure reset token
Stores the token (preferably hashed)
Stores an expiry time
Sends the email only if the account exists
Always returns the exact same response regardless of whether the email exists
Example response:
{
  "message": "If an account exists with that email, we have sent a reset link."
}
This prevents user enumeration attacks.
5. Await Email Sending
Verify every place that calls:
sendPasswordResetEmail(...)
properly uses:
await sendPasswordResetEmail(...)
Handle failures gracefully without crashing the application.
6. Environment Variables
Verify the project uses:
GMAIL_USER=
GMAIL_PASS=
APP_URL=
APP_URL should be used when generating password reset links instead of hard-coded URLs.
Remind me to configure these variables in:
.env.local
Vercel Project Environment Variables
7. Security Review
Review the implementation for security.
Verify that:
Password reset tokens are secure.
Tokens expire.
Sensitive values are never logged.
No information is leaked about whether an email exists.
Error messages are safe.
The implementation follows production best practices.
8. Testing
Confirm:
Email sends successfully.
HTML renders correctly.
Plain text fallback works.
Reset button opens the correct URL.
The application builds without TypeScript errors.
ESLint passes.
The implementation works on Vercel.
9. Deliverables
After completing the work, provide:
Complete updated lib/mailer.ts
Any modified route handlers
List of all changed files
Package installation commands
.env.local example
Summary of all changes
Confirmation that the project builds successfully with no TypeScript or lint errors.
Do not remove or break any existing functionality. Preserve the current project architecture and coding style while implementing these improvements.