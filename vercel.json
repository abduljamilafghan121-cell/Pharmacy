{
  "version": 2,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/db run migrate && pnpm --filter @workspace/api-server run build && BASE_PATH=/ PORT=3000 NODE_ENV=production pnpm --filter @workspace/web run build",
  "outputDirectory": "artifacts/web/dist/public",
  "functions": {
    "api/index.js": {
      "includeFiles": "artifacts/api-server/dist/**"
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/" }
  ]
}
